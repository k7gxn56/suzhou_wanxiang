今天的工作
1、查看并修复sct后台创建菜单功能bug，sctsystem数据库连接不正确，测试环境仿真环境使用同一套数据库，且菜单表有字段名与线上不一致。
2、修复目的地攻略全局搜索城市效果不理想的情况，因HotResult索引的mapping文件名称大小写不正确导致索引创建时结构不正确，导致导入的数据分词混乱。
3、与测试人员沟通搜狗xml项目上仿真事宜，发现一个问题，分支合主干需安装taskId来操作，每个项目的代码提交taskId一定要相同。
